# Environment variables for JIRA Status Automation Container

# Port inside the container that the web server listens on (default: 8080)
WEB_PORT=8080

# Port on the host machine to map to the container port (default: 8080)
HOST_PORT=8080

# Timezone for the container (default: UTC)
TZ=UTC

# Optional: Custom network settings
# PODMAN_NETWORK=jira-automation

# Optional: Custom volume paths
# CONFIG_PATH=./config
# REPORTS_PATH=./reports
# DATA_PATH=./data
