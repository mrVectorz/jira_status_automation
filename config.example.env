# Jira Status Automation - Environment Configuration Example
# Copy the variables you want to customize to your shell environment
# or create a .env file (not tracked in git)

# Backend Configuration
export BACKEND_HOST=0.0.0.0
export BACKEND_PORT=8000

# Frontend Configuration  
export FRONTEND_HOST=localhost
export FRONTEND_PORT=3001

# Additional CORS Origins (comma-separated)
# export CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3001

# API Configuration
export API_TITLE="Jira Status Automation API"
export API_DESCRIPTION="REST API for retrieving comprehensive Jira issue reports"
export API_VERSION=1.0.0

# Request timeout in seconds
export REQUEST_TIMEOUT=120

# Logging level (DEBUG, INFO, WARNING, ERROR)
export LOG_LEVEL=INFO

# Usage examples:
# 
# 1. Source this file to use all defaults:
#    source config.example.env
#    ./start.sh
#
# 2. Override specific variables:
#    export BACKEND_PORT=8001
#    export FRONTEND_PORT=3002
#    ./start.sh
#
# 3. Use with specific command:
#    BACKEND_PORT=8001 FRONTEND_PORT=3002 ./start.sh

# Common port alternatives:
# BACKEND_PORT=8001    # Alternative backend port
# FRONTEND_PORT=3002   # Alternative frontend port
# BACKEND_PORT=8080    # Production-like backend port

